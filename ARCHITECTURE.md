# 🏗️ System Architecture Overview

## Component Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         FRONTEND (Next.js)                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  🏠 Homepage          📅 Booking Page        👨‍💼 Admin Dashboard  │
│  (page.tsx)           (booking/page.tsx)     (admin/page.tsx)    │
│       │                     │                       │            │
│       │                     │                       │            │
│       └─────────┬───────────┴───────────────────────┘            │
│                 │                                                 │
│           🧭 Navbar Component                                     │
│           (Navbar.tsx)                                           │
│                 │                                                 │
└─────────────────┼─────────────────────────────────────────────────┘
                  │
                  │ HTTP Requests
                  │
┌─────────────────▼─────────────────────────────────────────────────┐
│                    API ROUTES (Next.js API)                       │
├───────────────────────────────────────────────────────────────────┤
│                                                                   │
│  📋 /api/admin/bookings        🎫 /api/admin/slots              │
│  ✅ /api/admin/bookings/:id/approve                               │
│  ❌ /api/admin/bookings/:id/reject                                │
│  📊 /api/admin/reports                                           │
│                                                                   │
└─────────────────┬─────────────────────────────────────────────────┘
                  │
                  │ Forward to Backend
                  │
┌─────────────────▼─────────────────────────────────────────────────┐
│                   BACKEND SERVER (Express.js)                     │
├───────────────────────────────────────────────────────────────────┤
│                                                                   │
│  📦 Public Routes:                                               │
│     POST /api/bookings          - Create booking                │
│     GET  /api/reviews           - Get reviews                   │
│     POST /api/reviews           - Create review                 │
│                                                                   │
│  🔐 Admin Routes:                                                │
│     GET  /api/admin/bookings              - Get all bookings    │
│     POST /api/admin/bookings/:id/approve  - Approve booking     │
│     POST /api/admin/bookings/:id/reject   - Reject booking      │
│     GET  /api/admin/slots                 - Get available slots │
│     GET  /api/admin/reports               - Generate reports    │
│                                                                   │
├───────────────────┬───────────────────────────┬───────────────────┤
│                   │                           │                   │
│                   ▼                           ▼                   │
│            📧 Email Service              🗄️ Database              │
│            (Nodemailer)                  (MongoDB)               │
│                   │                           │                   │
│            ┌──────┴──────┐            ┌──────┴──────┐            │
│            │             │            │             │            │
│        ✅ Approval   ❌ Rejection  📋 Bookings  ⭐ Reviews        │
│         Email       Email         Collection  Collection        │
│                                                                   │
└───────────────────────────────────────────────────────────────────┘
```

## Data Flow

### 1️⃣ User Makes a Booking

```
User (Browser)
    │
    │ Fills booking form
    │
    ▼
booking/page.tsx
    │
    │ POST request
    │
    ▼
Backend: POST /api/bookings
    │
    │ Save to database
    │
    ▼
MongoDB: Bookings Collection
    │
    └─► Status: "pending"
```

### 2️⃣ Admin Approves Booking

```
Admin Dashboard
    │
    │ Clicks "Approve"
    │
    ▼
admin/page.tsx
    │
    │ Shows modal for message
    │
    ▼
API: POST /api/admin/bookings/:id/approve
    │
    │ Forward to backend
    │
    ▼
Backend: Update booking status
    │
    ├─► Update MongoDB (status: "approved")
    │
    └─► Send Approval Email
         │
         ├─► Create HTML template
         ├─► Send via Nodemailer
         └─► Email delivered to user
```

### 3️⃣ Admin Rejects Booking

```
Admin Dashboard
    │
    │ Clicks "Reject"
    │
    ▼
admin/page.tsx
    │
    │ Shows modal for reason
    │
    ▼
API: POST /api/admin/bookings/:id/reject
    │
    │ Forward to backend
    │
    ▼
Backend: Update booking status
    │
    ├─► Update MongoDB (status: "rejected")
    │
    └─► Send Rejection Email
         │
         ├─► Create HTML template with reason
         ├─► Send via Nodemailer
         └─► Email delivered to user
```

### 4️⃣ View Available Slots

```
Admin Dashboard (Slots Tab)
    │
    │ Load slots
    │
    ▼
API: GET /api/admin/slots
    │
    │ Forward to backend
    │
    ▼
Backend: Calculate slots
    │
    ├─► Query all bookings (next 30 days)
    ├─► Generate time slots
    ├─► Calculate capacity per slot
    ├─► Count booked guests
    └─► Return available slots
         │
         ▼
    Display in UI with:
    - Green: >5 available
    - Yellow: 1-5 available
    - Red: Fully booked
```

### 5️⃣ Generate Reports

```
Admin Dashboard (Reports Tab)
    │
    │ Load reports
    │
    ▼
API: GET /api/admin/reports
    │
    │ Forward to backend
    │
    ▼
Backend: Aggregate data
    │
    ├─► Total bookings by status
    ├─► Bookings by branch
    ├─► Guest statistics
    ├─► Monthly trends
    └─► Recent activity
         │
         ▼
    Display in UI:
    - Summary cards
    - Charts & graphs
    - Export to CSV/JSON
```

## Technology Stack

### Frontend Layer
```
┌─────────────────────────────────────┐
│        Next.js 16 (React 19)        │
│  ┌───────────────────────────────┐  │
│  │       TypeScript               │  │
│  ├───────────────────────────────┤  │
│  │     Tailwind CSS               │  │
│  ├───────────────────────────────┤  │
│  │    Framer Motion               │  │
│  │    (Animations)                │  │
│  └───────────────────────────────┘  │
└─────────────────────────────────────┘
```

### Backend Layer
```
┌─────────────────────────────────────┐
│          Node.js + Express          │
│  ┌───────────────────────────────┐  │
│  │        Mongoose ODM            │  │
│  ├───────────────────────────────┤  │
│  │       Nodemailer               │  │
│  ├───────────────────────────────┤  │
│  │         CORS                   │  │
│  └───────────────────────────────┘  │
└─────────────────────────────────────┘
```

### Database Layer
```
┌─────────────────────────────────────┐
│             MongoDB                 │
│  ┌───────────────────────────────┐  │
│  │   Bookings Collection          │  │
│  │   - Schema validation          │  │
│  │   - Indexes on date/status     │  │
│  ├───────────────────────────────┤  │
│  │   Reviews Collection           │  │
│  │   - Rating system              │  │
│  │   - Branch filtering           │  │
│  └───────────────────────────────┘  │
└─────────────────────────────────────┘
```

## Security Considerations

```
⚠️  NO AUTHENTICATION (As Requested)
│
├─► For Production, Add:
│   ├─► JWT Authentication
│   ├─► Role-Based Access (RBAC)
│   ├─► Rate Limiting
│   ├─► HTTPS/SSL
│   ├─► Input Validation
│   ├─► XSS Protection
│   └─► CSRF Tokens
```

## Deployment Architecture

```
┌──────────────────────────────────────────────────────────┐
│                    PRODUCTION                             │
├──────────────────────────────────────────────────────────┤
│                                                           │
│  Frontend (Vercel)              Backend (Railway)        │
│  ┌──────────────┐               ┌──────────────┐        │
│  │   Next.js    │──────────────▶│   Express    │        │
│  │  Static +    │   HTTPS       │   Server     │        │
│  │  SSR Pages   │               │              │        │
│  └──────────────┘               └──────┬───────┘        │
│         │                              │                 │
│         │                              │                 │
│         │                       ┌──────▼───────┐        │
│         │                       │   MongoDB    │        │
│         │                       │   Atlas      │        │
│         │                       └──────────────┘        │
│         │                                                │
│         │                       ┌──────────────┐        │
│         └──────────────────────▶│    Gmail     │        │
│                                 │    SMTP      │        │
│                                 └──────────────┘        │
│                                                           │
└──────────────────────────────────────────────────────────┘
```

## File Structure

```
moon-restaurant-nextjs/
├── app/
│   ├── admin/
│   │   └── page.tsx                 # 👨‍💼 Admin Dashboard UI
│   ├── api/
│   │   └── admin/
│   │       ├── bookings/
│   │       │   ├── route.ts         # 📋 Fetch bookings
│   │       │   └── [id]/[action]/
│   │       │       └── route.ts     # ✅❌ Approve/Reject
│   │       ├── slots/
│   │       │   └── route.ts         # 🎫 Available slots
│   │       └── reports/
│   │           └── route.ts         # 📊 Reports
│   ├── booking/
│   │   └── page.tsx                 # 📅 Public booking form
│   └── page.tsx                     # 🏠 Homepage
│
├── backend/
│   ├── server.js                    # 🚀 Main backend server
│   ├── package.json                 # 📦 Dependencies
│   ├── .env.example                 # 🔑 Config template
│   └── README.md                    # 📖 Backend docs
│
├── components/
│   ├── Navbar.tsx                   # 🧭 Navigation
│   ├── Icon.tsx                     # 🎨 Icon system
│   └── ...
│
├── QUICK_START.md                   # ⚡ Quick setup guide
├── ADMIN_DOCUMENTATION.md           # 📚 Full documentation
├── setup.ps1                        # 🪟 Windows setup
└── setup.sh                         # 🐧 Linux/Mac setup
```

## Environment Variables

```
Frontend (.env or .env.local):
┌─────────────────────────────────────┐
│ NEXT_PUBLIC_API_URL                 │
│ = http://localhost:5000/api         │
└─────────────────────────────────────┘

Backend (backend/.env):
┌─────────────────────────────────────┐
│ MONGODB_URI                         │
│ EMAIL_USER                          │
│ EMAIL_PASSWORD                      │
│ PORT                                │
│ FRONTEND_URL                        │
└─────────────────────────────────────┘
```

## Key Features

```
✅ Booking Management
   ├─ View all bookings
   ├─ Filter by status
   ├─ Search functionality
   └─ Approve/Reject with messages

📧 Email System
   ├─ HTML templates
   ├─ Approval notifications
   ├─ Rejection with reasons
   └─ Professional design

🎫 Slot Management
   ├─ 30-day availability
   ├─ Real-time calculation
   ├─ Visual indicators
   └─ Capacity tracking

📊 Reports & Analytics
   ├─ Summary statistics
   ├─ Branch breakdown
   ├─ Export CSV/JSON
   └─ Activity feed
```

---

**Built for Moon Restaurant with ❤️**
